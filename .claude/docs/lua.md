# Lua

## Formatting

`stylua` — Lua formatter. Referenced in neovim ftplugin config.

## LSP

Neovim uses `lua_ls` with Neovim runtime awareness (LuaJIT, vim runtime files). When editing neovim Lua config, respect `.luarc.json` / `.luarc.jsonc` if present.
