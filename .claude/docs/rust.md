# Rust

## Linting

`cargo clippy` — Rust linter. Neovim uses `rust_analyzer` LSP which surfaces clippy diagnostics.

Run after code changes in Rust projects.

## Formatting

`cargo fmt` — standard Rust formatter.
